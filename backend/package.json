{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "cross-env NODE_ENV=test dotenv -e .env.test -o -- mocha tests/*.test.js tests/teardown.js --timeout 10000",
    "test:auth": "cross-env NODE_ENV=test dotenv -e .env.test -o -- mocha tests/auth.test.js tests/teardown.js --timeout 10000",
    "test:category": "cross-env NODE_ENV=test dotenv -e .env.test -o -- mocha tests/category.test.js tests/teardown.js --timeout 10000",
    "test:note": "cross-env NODE_ENV=test dotenv -e .env.test -o -- mocha tests/note.test.js tests/teardown.js --timeout 10000"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "mongoose": "^9.9.3",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0"
  },
  "devDependencies": {
    "chai": "^4.3.10",
    "chai-http": "^4.4.0",
    "cross-env": "^10.1.0",
    "dotenv-cli": "^11.0.0",
    "mocha": "^10.2.0",
    "nodemon": "^3.1.14"
  }
}